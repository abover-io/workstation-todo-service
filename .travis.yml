languange: node_js

services: 
  - docker

node_js:
  - lts/*

before_install:
  - docker-compose up -d --build

script:
  - docker start `docker ps | grep "fancy-todo-api_api_1" | awk '{ print $1 }'`
  - docker exec -it `docker ps | grep "fancy-todo-api_api_1" | awk '{ print $1 }'` npm test

after_script:
  - docker-compose down
